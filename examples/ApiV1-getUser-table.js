// Generated by CoffeeScript 1.6.3
(function() {
  var ApiV1, Table, api, apiKey,
    _this = this;

  ApiV1 = require('..').ApiV1;

  apiKey = require('./credentials').apiKey;

  Table = require('cli-table');

  api = new ApiV1({
    apiKey: apiKey
  });

  api.user.getUser(function(err, user) {
    var entry, key, table, value;
    if (err) {
      return console.error(err);
    }
    table = new Table();
    for (key in user) {
      value = user[key];
      entry = {};
      entry[key] = "" + value;
      table.push(entry);
    }
    return console.log(table.toString());
  });

}).call(this);
