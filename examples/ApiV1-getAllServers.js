// Generated by CoffeeScript 1.6.3
(function() {
  var ApiV1, api, apiKey,
    _this = this;

  ApiV1 = require('..').ApiV1;

  apiKey = require('./credentials').apiKey;

  api = new ApiV1({
    apiKey: apiKey
  });

  api.server.getAllServers(function(err, servers) {
    var server, server_id, _i, _len, _results;
    if (err) {
      return console.error(err);
    }
    _results = [];
    for (_i = 0, _len = servers.length; _i < _len; _i++) {
      server = servers[_i];
      server_id = parseInt(server.$ref.split('/').slice(-1)[0]);
      _results.push(api.server.getServerInfo(server_id, function(err, info) {
        return console.log(err, info);
      }));
    }
    return _results;
  });

}).call(this);
